apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: app-2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: mydockerhub/react-frontend:1.0
          env:
            - name: API_URL
              value: http://backend:5000
          ports:
            - containerPort: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: app-2
spec:
  selector:
    app: frontend
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
